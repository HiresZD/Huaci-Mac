# 更新日志

按版本倒序记录；历史条目描述对应版本当时的行为，当前用法见 [使用说明](docs/USER_GUIDE.md)。

## 1.3.7 音标对齐与细滚动条

- **小喇叭按文字基线对齐**：音标测宽和测高统一使用文本控件的度量方式，喇叭对齐第一行文字的视觉中心；长音标换行时仍跟随第一行。保留 24 × 24 pt 点击区域，并按实际文字和基线计算上下留白。
- **翻译结果使用低对比滚动条**：默认显示 3 pt 淡细线，鼠标移入或拖动时变为 6 pt 并加深，无底色轨道；仍保留原生宽度的操作区域及拖动、点击滚动功能。内容无需滚动时隐藏，不修改系统滚动条偏好。
- **正文预留间距**：给滚动条留出空间，避免遮挡译文；滚动条显隐不会改变正文折行宽度。

当前环境没有 macOS SDK，已完成源码复核和打包校验，未进行本版原生编译或界面交互实测。请在 Mac 上构建后检查短／长音标、滚动条悬停与拖动效果。

## 1.3.6 浮窗圆角透明修复

- **圆角外背景透明**：针对「翻译 / 问 AI」划词浮条底部出现白色尖角的问题，给磨砂背景增加真实圆角遮罩，同时保留内容层裁剪；翻译结果浮窗共用这一修复。
- **尺寸变化同步更新**：浮条切换为结果、内容增减和屏幕缩放变化时重新适配遮罩并刷新窗口阴影；保留原有磨砂效果、按钮和自适应尺寸。

当前环境没有 macOS SDK，已完成源码复核，尚未进行原生编译和透明边缘实测。请在 Mac 构建后检查白色、深色背景下的浮条及短长翻译结果。

## 1.3.5 恢复系统发音与展开预设

- **恢复系统发音**：小喇叭直接使用 Mac 已安装的英文语音，移除 Merriam-Webster 查询、音频下载、来源选择、词典密钥和注册入口。升级后自动使用系统语音，保留原有英式／美式口音偏好；旧词典密钥不再读取。
- **精简语音设置**：语音分区仍在设置页底部，保留口音选择、试听和停止。口音修改立即保存；重复点击、切换词条、关闭窗口时停止旧播放，设置试听与词典朗读互相停止。
- **三个预设一键发送**：问 AI 输入区上方并排显示「解释这段」「提炼要点」「润色表达」。点击后立即按预设提问，无需再点发送；优先处理输入框里的文字，没有输入时使用本次划词。没有可用文本时提示补充，不发送占位内容；回答生成期间禁用预设按钮，避免重复请求。

本版删除了在线发音相关源码，请解压到新的文件夹后运行 `Build.command`，不要合并到旧源码目录。应用升级会沿用原有配置。当前环境没有 macOS SDK，未执行本版原生编译和发音、界面实测；Mac 构建脚本会进行完整类型检查及本地回归检查。

## 1.3.4 精简设置与自动保存

- **说明按需显示**：次要说明收进对应设置旁的信息图标，鼠标停留约 0.35 秒显示，移开或滚动后收起；也可点击查看。保留授权状态、缺少词典密钥、输入错误及连接测试可能消耗额度等关键提示。
- **缓存提示可用**：「最大条数」与「文本总量」改用主动悬停弹层，说明默认值、调整效果和生效时机；文本总量限制缓存数据，不是整个 App 的内存上限。
- **语音设置移到底部**：「英文单词发音」成为最后一个设置分区，保留来源选择、注册入口、保存和试听功能。
- **缓存与历史自动保存**：移除「保存缓存与历史设置」按钮。开关立即生效；数字在按回车、结束编辑、关闭设置或正常退出 App 时保存。编辑中的中间数值不会触发缓存缩减；无效输入显示错误并保留已保存值，关闭或退出前需修正。

当前环境没有 macOS SDK，未进行本版原生编译和界面交互实测。`Build.command` 会先检查全部应用源码，再运行本地回归检查；请在 Mac 上确认悬停提示和数字自动保存的效果。

## 1.3.3 构建修复

- **修复编译失败**：翻译历史窗口补上 `NSSearchFieldDelegate` 协议声明，解决搜索框委托赋值的类型错误。
- **处理构建警告**：明确辅助功能对象和数组的类型转换、划词读取任务的弱引用；权限重置轮询改为异步等待，并在异常退出时清理仍在运行的子进程。
- **提前检查完整应用**：`Test.command` 在核心回归检查前，对全部应用源码（包括 AppKit 界面）进行类型检查，避免仅核心检查通过后才发现界面编译错误。`Build.command` 会自动执行这一检查。

当前交付环境没有 macOS SDK，未完成本版原生编译验证；请在 Mac 上运行 `Build.command` 进行完整检查和构建。

## 1.3.2 Merriam-Webster 词典发音

- **默认使用 Merriam-Webster**：设置新增「发音来源」，默认 Merriam-Webster 词典，可选 macOS 系统语音；从旧版升级也默认使用 Merriam-Webster。系统语音仍可选择美式或英式口音，无需词典密钥。来源和口音修改后，点击「保存发音设置」。
- **注册提醒与快捷入口**：设置的「英文单词发音」提供独立密钥输入框和「注册并获取 API Key…」按钮。点击后在默认浏览器打开[官方注册页](https://dictionaryapi.com/register/index)，选择 **Collegiate Dictionary**，完成邮件验证，再登录 **My Keys** 复制对应密钥并保存。尚未配置密钥时有醒目提示；此时点击词典中的小喇叭，会直接打开这部分设置。个人非商业用途目前每个密钥每天最多 1,000 次查询，具体以[官方说明](https://dictionaryapi.com/)为准。
- **可先试听再保存**：「试听 apple」使用当前填写的语音配置，试听本身不保存设置；支持停止试听。Merriam-Webster 播放词典提供的原词音频，不提供统一的英音／美音切换；没有准确词条、对应词形录音或请求失败时会提示，可自行切换系统语音。
- **只在点击时查询**：查看词典、翻译和打开设置不会发起韦氏查询，点击喇叭或试听才请求所读单词。朗读英文原词，目标语言切换不改变所读词汇。快速重复点击、换词、重试、关闭窗口或更改语音配置会取消旧请求或停止播放，设置试听和词典播放互相停止，避免串音。
- **密钥与缓存独立**：词典密钥保存在系统钥匙串，不混入大模型 API 配置、翻译历史或导出文件；选择系统语音不需要读取词典密钥。每个播放入口仅在内存中保留最近一条成功音频（单条最多 2 MB），不写入磁盘，也不占用翻译文本缓存的 5 MB 配额。更改或删除词典密钥会清理旧发音缓存。

发音来自 [Merriam-Webster 官方音频接口](https://dictionaryapi.com/products/json#sec-2.prs)。本版按返回顺序选择匹配词形的首个可用发音；多音词暂未提供读音选择菜单，也不保证与大模型给出的每个音标或释义一一对应。Merriam-Webster 的标音格式不会替换现有 IPA 显示。当前交付环境无 macOS SDK，也没有真实词典密钥，因此未进行原生编译、真实接口和声音播放实测；新增的模拟回归检查会由 Mac 上的 `Build.command` 先执行。

## 1.3.1 音标与发音按钮对齐

- 修复词典头部小喇叭相对音标偏下的问题：音标按实际文字高度显示，与发音按钮在独立行内垂直居中。
- 保留按钮的 24 × 24 pt 点击范围和 6 pt 左右间距；较长音标仍可换行，词典头部按内容调整高度。

## 1.3 多套 API、翻译缓存、历史收藏与 AI 预设

- **多套 API 配置**：设置中可以命名、添加、编辑、删除配置，分别指定「翻译」和「问 AI」使用哪一套。原有地址、模型与密钥自动沿用为「默认配置」，不用重新填写；每套密钥分别放在系统钥匙串中。余额查询和连接测试使用设置里当前编辑并已保存的配置。
- **翻译缓存**：默认开启，最多 **200 条、5 MB 文本数据**。同一原文、目标语言及 API 配置的成功结果可直接复用，包括完整词典和发音入口；失败、取消和未完成的结果不缓存。命中时显示「来自缓存」，不请求模型；「重试」始终绕过缓存。更改 API 配置或密钥会清空缓存，防止复用旧账户结果。
- **缓存上限可调**：在设置的「翻译缓存与历史」中修改，条数范围为 **1–10,000**，文本大小范围为 **1–100 MB**，单独点击保存。两项右侧的 ⓘ 图标提供鼠标悬停说明。两个上限同时生效，达到任一上限就移除最久未使用的条目；单条结果过大时仍正常显示，只是不缓存。降低上限立即生效，不会预先分配上限大小的内存。文本预算按 UTF-8 编码的原文、译文、词典与缓存元信息计算，**不等于整个 App 的内存占用**。缓存只在本次运行保留，可一键清空；关闭缓存也会清空它。
- **翻译历史与收藏**：菜单栏和设置中均可打开。成功翻译默认自动保存到本机，结果窗口的星标可收藏。支持搜索、查看原文和译文、离线重新展示结果、复制、删除，以及把当前筛选列表导出为 `.md`。关闭自动历史后不再新增查询记录，仍可手动收藏；清空缓存不删除历史，清空历史保留收藏。相同原文与目标语言再次查询会更新该条结果并保留收藏状态。
- **本地记录有上限**：保留最近 200 条未收藏记录、最多 1,000 条收藏，合计最多 20 MB 文本数据，与缓存上限分别计算。空间不足时优先移除旧的未收藏记录，已有收藏不会被自动删除；保存失败会提示并保留原记录。文件位于 `~/Library/Application Support/Huaci/translation-history.json`，内容包括原文、译文、目标语言、时间和模型名称，不含 API Key；退出后仍可查看。AI 对话仍沿用原来的当前会话规则，不自动加入翻译历史。
- **AI 预设指令**：对话输入栏新增「解释这段」「提炼要点」「润色表达」。先使用输入框文字，输入框为空则使用本次划词；选择后填入可编辑提示词，点击发送或按 ⌘Return 才请求模型。连续切换未修改的预设不会重复嵌套提示词。划词弹出的两个按钮和「问 AI」立即发送行为保持原样。

本次不加入 OCR，也不改变各 App 的选区读取与复制兼容方式。已添加配置迁移、密钥隔离、缓存淘汰、记录持久化与 AI 预设的回归用例；交付环境没有 macOS SDK，原生编译与界面仍需在 Mac 上验证。`Build.command` 会先执行回归检查再编译。

## 1.2.2 译文字号与问 AI 窗口定位

- **短译文更醒目**：普通翻译结果按实际排版长度选择 20、18、16、14 pt 四档字号，内容越多字号越小，14 pt 为原有字号下限。流式输出合并调整；重新翻译短内容时恢复较大字号。「无需翻译」显示的原文也使用这一规则。
- **新提问跟随当前选区**：每次划词点击「问 AI」时，窗口移动到本次选区附近；跨屏选词时使用选区所在屏幕，并限制在可见区域内。
- **连续追问保留位置**：窗口内继续提问、接收回答或从菜单打开当前对话时，沿用当前窗口位置；用户调整的窗口尺寸也保留，只有超出屏幕可见范围时才缩小。

## 1.2.1 拖拽安装窗口

- **构建完成直接打开安装窗口**：`Build.command` 会在 `dist` 中生成 `Huaci-Installer.dmg` 并打开。窗口使用绿色标题区与简洁背景，左侧为 Huaci，右侧为「应用程序」入口，中间有拖拽箭头和中文安装提示。
- **拖拽即可安装或升级**：将左侧 Huaci 拖到右侧，复制完成后从「应用程序」打开；升级时按 Finder 提示替换原应用。安装完成后可推出安装磁盘。
- **失败时仍可手动安装**：制作 DMG 失败时保留已经构建好的 `dist/Huaci.app`，并在 Finder 中定位它；不会打开旧安装包。系统不允许布置 Finder 窗口时，会生成带「安装说明.txt」的基础安装磁盘，仍可拖到 Applications。

首次制作时，macOS 可能询问是否允许终端控制 Finder，用于设置这次安装窗口的背景和图标位置。安装背景已随源码提供，构建无需下载素材或安装额外工具。

## 1.2 API 余额与账户入口

- **设置页新增「API 余额」**：根据 API 地址识别服务商，使用已保存的配置；模型名为 GPT 或 DeepSeek 不会改变余额所属平台。
- **DeepSeek 官方余额**：打开设置或成功保存配置后查询一次，也可手动刷新。按接口返回的人民币／美元分别显示可用余额与更新时间；金额保留接口精度，详情提供赠金和充值余额。查询不发送划词、对话内容或模型请求，也不打断翻译和问答。
- **OpenAI 官方账单入口**：识别 `api.openai.com` 后，显示应用内余额查询的限制，并提供「前往官方账单」按钮。**本版不能在应用内显示 OpenAI 官方剩余余额数字。** 截至 2026-09-18 查阅的官方公开文档，没有找到普通模型 API Key 可用的充值余额接口；[Costs API](https://developers.openai.com/api/reference/resources/admin/subresources/organization/subresources/usage/methods/costs) 返回历史消费，示例使用单独的管理员密钥，不能当作剩余余额。
- **中转站与其他服务**：显示暂未适配，翻译和问答仍可用；不根据模型名猜测官方余额接口，不向另一服务商发送中转站密钥。后续可根据具体平台的余额接口文档添加适配。
- **失败与切换保护**：查询失败会显示原因，已有金额保留原更新时间并标注更新失败；不会用零代替未知余额。编辑地址、模型或密钥后清除旧结果，保存成功后再查询；关闭设置会取消请求，旧请求不能覆盖新账户数据。没有后台轮询，余额只在设置窗口中使用并暂存于内存。

DeepSeek 使用其[官方余额接口](https://api-docs.deepseek.com/zh-cn/api/get-user-balance/)，显示的是密钥所属账户余额，同一账户在其他应用中的消费也会反映在这里。OpenAI 账单按钮打开默认浏览器，可能需要登录及相应组织的账单权限；应用不读取浏览器登录状态。

## 1.1.9 英文系统发音与构建窗口关闭

- **音标旁新增小喇叭**：英语单词的完整词典结果显示后，点击即可使用 Mac 已安装的系统语音朗读原词；不额外调用 DeepSeek，也不需要新的 API Key。切换中文、韩语或阿拉伯语等目标语言时，仍读英文原词。非英语或源语言不确定的词条不显示英文朗读按钮。
- **英美口音可选**：设置中新增「英文单词发音」，默认美式英语，可切换英式英语并自动保存；下次点击喇叭时生效。只选对应口音的已安装语音，缺少时提示去系统设置下载，不悄悄换成其他口音。macOS 14 及以后还会排除个人语音和趣味语音。
- **播放与窗口同步**：再次点击会停止旧播放并重新读一遍，不积累播放队列。切换词条、目标语言、重试、关闭翻译窗口或退出应用都会停止当前发音。语音不可用时只提示发音问题，不清空已有词典结果。
- **词典头部保持可见**：原词、音标和发音按钮留在释义上方，长释义独立滚动；窗口继续按内容调整高度，复制仍包含原词、音标和全部解释。
- **修复构建后回车不关闭**：成功构建后，在 Apple Terminal 中按回车会尝试关闭本次构建所在的单标签窗口，兼容双击脚本与手动 `bash Build.command`。脚本只关闭精确匹配且已空闲的窗口；其他标签页、后台任务或 Terminal 自动化未获允许时保留窗口，可按 **⌘W** 手动关闭。构建失败不会触发自动关闭，便于保留报错。

发音基于 [Apple 系统语音合成](https://developer.apple.com/documentation/avfaudio/avspeechsynthesizer)，普通系统语音不需要麦克风权限。可在系统设置的辅助功能朗读选项中[下载更多语音](https://support.apple.com/guide/mac-help/change-the-voice-your-mac-uses-to-speak-text-mchlp2290/mac)。这是原词的系统朗读，不保证与模型给出的每个音标或多音词义项严格一一对应。

## 1.1.8 稳定词典与固定窗口

- **英文单词固定走词典查询**：App 根据原始选区识别单词、缩写和连字符词，切换目标语言或重试时仍使用同一个原词。多个词或句子继续直接翻译。目标语言与源语言相同则仍优先提示无需翻译。
- **统一显示顺序**：顶部用较大的字体显示原词，下方依次为音标、词性与释义、常见搭配、例句。词性、释义和搭配／例句译文使用目标语言，原文保留源语言。阿拉伯语解释使用从右到左的段落方向。
- **完整词条校验**：英文单词查询使用固定 JSON 数据结构，收到完整且校验通过的词条后再显示，不再把单个译词当作完整词典结果。阿拉伯语、韩语、中文等目标使用同一结构。普通英文词要求给出 IPA；确实无法确认时，音标栏显示「暂无可靠音标」，不编造读音。
- **格式自动修复一次**：完整响应缺少词典栏目或格式错误时，最多额外请求一次完整词条；这次请求可能产生额外费用。仍不完整则显示错误并提供手动重试。网络、接口、超时或取消不会自动重发。模型的音标准确性、词义和语种仍需按实际输出判断，结构校验不能核实全部内容。
- **图钉固定翻译窗口**：点击右上方图钉后，点击其他应用、打字或滚动页面都保留当前窗口；拖动标题栏可移动。固定期间其他选区不会覆盖当前内容，需要查看新选词时先取消固定或关闭。切换目标语言和重试保留固定状态与位置；× 或 Esc 仍可关闭。暂停助手或保存接口设置会结束当前翻译并关闭窗口。

本次没有更改原有自动取词与复制兼容规则。词典加载期间先显示原词，收到完整词条后统一排版；句子翻译和 AI 问答仍逐步显示。

## 1.1.7 独立会话与记录导出

- 采用方案 A：每次划词点击 **「问 AI」** 都开始新会话，不带入上一段问答；当前窗口里的手动追问仍延续本次会话。若旧回答仍在生成，接受新问题后停止旧请求。新问题或接口配置校验失败时，保留原会话。
- 对话窗口的 **「新对话」** 按钮改为 **「清除记录」**：清空当前已发送的问答并停止生成，之后旧请求的返回不会再写入窗口。尚未发送的输入草稿保留。
- 新增 **「导出记录」**：选择保存位置，将点击按钮时的当前会话保存为 UTF-8 **`.md`** 文件，包括提问、回答、追问、导出时间和完成状态。生成中也可导出，只保存当时已收到的部分，并标注未完成；导出不打断回答。
- 导出仅包含当前已发送会话，不包括输入草稿、系统提示词和接口配置。正文用 Markdown 代码围栏保留原始文字、换行和代码，与应用内的纯文本呈现一致；回答中的未闭合代码块不会破坏后续轮次。
- 没有记录时，清除和导出按钮不可用。取消保存不创建文件；覆盖已有文件由系统保存窗口确认，写入失败会显示错误。

旧会话不会自动存档。需要保留时，请在下一次划词提问或清除记录之前导出。关闭对话窗口后，从菜单栏重新打开可查看当前会话；下一次划词提问才会开始新的会话。

## 1.1.6 同语言跳过与菜单栏显示

- 选项按钮统一为 **「翻译」**，选择其他目标语言也保持这个名称，目标语言在结果窗口下拉框中查看和修改。
- 本机可靠识别原文已经是目标语言时，展示原文并提示 **「无需翻译」**，不读取 API 密钥、不发送请求；单个词也不再展开同语言词典。识别在后台进行，切换选区或目标语言后丢弃旧结果。
- 短词歧义、较长文本、混合语言或判断不可靠时，继续由同一次模型请求处理；提示词也要求同语言无需翻译，避免误把外语内容跳过。本机语言识别和模型判断都可能出错。需要简繁转换时仍正常处理。单词需要释义时可使用「问 AI」。
- 设置新增 **「显示菜单栏图标」**，默认开启并沿用旧版的可见入口。取消后会保持划词助手开启，并禁用设置中的停用选项；如果先暂停再隐藏图标，会自动恢复划词。重新显示图标后可按需暂停。
- 隐藏图标后，重新双击安装的 **Huaci.app** 即可打开设置；应用窗口中可使用 **⌘,** 打开设置。设置底部新增 **「退出划词助手」**。切换图标显示不会中断正在进行的 AI 回答。
- 1.1.6 当时沿用当前会话；1.1.7 已改为每次划词新建，并提供清除与导出按钮，没有历史列表。

## 1.1.5 恢复自动划词

修复 1.1.4 自动取词可能退化、导致原来可直接划词的应用也需要按 ⌘C 的问题。原因是扩展读取之前的控件检查可能提前结束整个流程，或先耗尽读取时间，原本有效的焦点选区没有机会被读取。

- 恢复原有焦点控件与附近父控件的直接选区读取，成功后立即弹出按钮；无需先完成鼠标命中和整条控件链的检查。
- 直接读取失败后，再尝试新增的鼠标、选区范围和局部子控件读取，两条路径使用各自的时间预算。
- **兼容模式仅增加复制入口，不关闭自动划词。** 开启或关闭它，Chrome、ChatGPT 等原本支持的界面都应继续自动弹框；只有自动取不到时，才需要按 ⌘C。
- 保留已识别密码框的阻断、文本长度限制，以及切换应用或改变选区后丢弃过期读取结果。未改动 API 配置、语言、词典或对话行为。

这次修复基于旧版与新版的源码对比；交付环境仍没有 macOS，Chrome 和 ChatGPT 的实际恢复情况需要在 Mac 上验证。

## 1.1.4 跨应用取词兼容

- 扩展自动读取：除了当前焦点控件，还尝试鼠标所在控件、附近的文字子控件，以及应用提供的选区范围。1.1.5 将这部分调整为直接取词失败后的后备路径；在扩展路径中优先尝试鼠标所在控件。部分阅读器更新选区较慢，会在选区仍有效时重试一次。
- 新增设置 **「复制后显示划词选项（兼容模式）」**，默认关闭。开启后，在微信、Zotero、福昕等应用遇到选词不弹框时，选中文字再按 **⌘C**；检测到新复制的文字后，显示翻译和「问 AI」选项。只有点击选项才发送接口请求。
- 保留自动调整翻译窗口、单词词典、目标语言选择，以及「问 AI」立即发送和后续对话。

这能扩大兼容范围，但不能保证每个应用或文档都能自动弹框。应用需要提供可读取的辅助功能选区，或者允许复制出文字；图片、扫描 PDF 和禁止复制的内容仍需要其他取字方式，本版没有 OCR。Apple 的说明见 [自定义控件的辅助功能接口](https://developer.apple.com/library/archive/documentation/Accessibility/Conceptual/AccessibilityMacOSX/ImplementingAccessibilityforCustomControls.html)。微信、Zotero、福昕的具体版本尚未在交付环境中实测。

## 1.1.3 显示与提问

- 翻译结果框根据正文排版自动调整高度：短译文紧凑展示，词典解释和长译文逐渐展开。到达屏幕可见范围内的高度上限后，使用内部滚动。流式输出合并调整，避免每个字都触发窗口跳动。
- 点击「问 AI」立即发送当前选中文字，不再需要在对话窗口中重复点击发送。输入框用于后续追问，按发送按钮或 ⌘Return 提交。
- 1.1.3 增加了连续提问；1.1.7 起，新划词提问开始独立会话，窗口内手动追问才延续本次问答。原来尚未发送的草稿继续保留在输入框中，不会跟随选中文字一起自动发送。

## 1.1.2 单词词典模式

划词后点击「翻译」，无须切换模式。以下词典与翻译规则适用于原文和目标语言不同的情况；1.1.6 起，同语言优先提示无需翻译：

| 选中文字 | 处理方式 |
| --- | --- |
| `apple`、`run`、`well-known` | 较大的原词、固定音标栏、词性和释义、常见搭配、1–2 个例句及译文 |
| `ice cream`、`a beautiful day` | 直接翻译短语，不逐词展开 |
| `I like apples.` | 只输出句子译文 |
| `苹果`、`猫` | 目标为其他语言时按单词处理；目标仍为对应中文时提示无需翻译 |

词典的说明、释义和例句译文使用你选择的目标语言，原词与原文例句保留原语言。网址、代码、纯数字和技术标识符不编造词条。1.1.8 起，可识别的英文形态单词在本机进入结构化词典模式，不额外发送分类请求；其他文字仍由同一次模型请求判断词与句，有歧义时优先直接翻译。模型可能判断错误，词典内容并非经过核验的权威词典数据。

## 1.1.1 权限修复

修正了后台权限变化未同步到设置页的问题。现在设置页与菜单使用同一状态，定时检查在菜单打开时也继续运行；从系统设置返回或点击「重新检测」会重新安装事件监听，并清除此前失败的浏览器辅助功能初始化状态。

**系统开关已开，但仍提示当前 App 未获授权时：**

1. 先退出旧版，构建并把新版本放到固定的「应用程序」位置，替换后只打开这一份。
2. 点击设置里的 **「重新检测」**。若已授权，直接到其他应用选中文字测试。
3. 若仍未获授权，点击 **「修复授权… → 定位当前 App」**。系统设置中的权限页面可能叫「设备控制和数据访问」，旧版叫「辅助功能」。在该列表中选中旧 Huaci，用减号移除，再用加号添加 Finder 中选中的当前这份 Huaci，并开启开关。
4. 回到应用点「重新检测」。若系统仍保留异常记录，可在「修复授权」中选择 **「重置本 App 授权…」**，确认后再添加当前 App。
5. 如果重新授权后仍未生效，使用「修复授权 → 退出后重新打开…」。Finder 会定位当前应用，退出后双击它；退出会结束请求并清空内存中的对话，配置和密钥保留。

修复窗口显示实际运行位置和版本，并提示同一应用的多个运行副本。重置只调用系统工具清除 `com.local.huaci-assistant` 的 Accessibility 授权，必须由你重新开启；不会重置其他应用、修改系统权限数据库或把未授权显示成已授权。

权限列表开关和当前进程检查不一致，不足以确定唯一原因。旧版使用临时签名，每次重新构建后身份可能变化，系统可能保留旧授权；单纯改刷新代码无法保证修复这类记录。Apple 说明系统使用代码签名识别应用身份，参考 [代码签名说明](https://developer.apple.com/library/archive/technotes/tn2206/_index.html) 与 [Apple 工程师对 TCC 的说明](https://developer.apple.com/forums/thread/703188)。
